import { extendTheme } from "@chakra-ui/react";
import { mode } from "@chakra-ui/theme-tools";

const styles = {
  styles: {
    global: {
      "html, body, #root": {
        height: "100%"
      }
    }
  }
};

export { styles };
